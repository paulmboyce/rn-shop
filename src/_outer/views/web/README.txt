This is where we would put our React Web components.

We could then reuse ALL our:
- Redux, 
- Actions, 
- Controllers, 
- Presenters, etc.

Providing a Web App would be minimal coding.

We would simply rewrite the native JSX/styles of 
the equivalent native components into web JSX (~HTML/css).